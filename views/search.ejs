<div class="column">
    <h4>
        <%= ('' + points.length) %>
        <%= (1 == points.length) ? 'match' : 'matches' %>
    </h4>

    <div>
    <% points.forEach(function (p) { %>
        <%- partial('point', { point: p }) %>
    <% }); %>
    </div>
</div>
